<% @lists.each do |list| %>
  <%= link_to list.name, list_path(list), class: "list" %>
  <%= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %>
<% end %>
<%= link_to "+ Add a list", new_list_path, class: "add" %>
